---
label: section
hide_body: true
fields:
- name: titre
  type: text
  config:
    required: false
  label: titre
- name: professeur
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: data
  label: professeur
